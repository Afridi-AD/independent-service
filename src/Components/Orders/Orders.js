import { getAuth } from 'firebase/auth';
import React from 'react';
import {useAuthStare} from 'react-firebase-hooks/auth'








const Orders = () => {
    
    return (
        <div>
            
        </div>
    );
};

export default Orders;